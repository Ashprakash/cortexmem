services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: cortexmem
      POSTGRES_USER: cortexmem
      POSTGRES_PASSWORD: cortexmem
    ports:
      - "5432:5432"
    volumes:
      - cortexmem-data:/var/lib/postgresql/data

volumes:
  cortexmem-data:
