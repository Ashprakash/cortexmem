# engram
An open-source MCP (Model Context Protocol) server that gives AI coding agents persistent memory across sessions. This document contains everything you need to build v0.1 from scratch and publish it as an npm package.
